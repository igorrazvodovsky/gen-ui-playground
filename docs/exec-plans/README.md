# Execution plans

Use this directory for longer-lived implementation plans once work exceeds a small local change.

Recommended shape:

- problem statement,
- scope and non-goals,
- numbered execution steps,
- decision log,
- verification notes,
- and what changed in repo documentation as a result.

For now, `docs/PLANS.md` is enough because the repo is still at bootstrap stage.

